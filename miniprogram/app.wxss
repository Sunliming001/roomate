/* app.wxss */
page {
  background-color: #F2F2F7; /* iOS 默认背景灰 */
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', Helvetica, Segoe UI, Arial, Roboto, 'PingFang SC', 'miui', 'Hiragino Sans GB', 'Microsoft Yahei', sans-serif;
  color: #1C1C1E; /* iOS 默认黑 */
  font-size: 30rpx;
  line-height: 1.5;
  
  /* 定义全局变量 */
  --primary-color: #FF2442; /* 你的主色调 */
  --bg-color: #F2F2F7;
  --card-bg: #FFFFFF;
  --text-main: #1C1C1E;
  --text-sub: #8E8E93;
  --separator: #E5E5EA;
  --radius-lg: 24rpx;
  --radius-md: 16rpx;
  --shadow: 0 4rpx 24rpx rgba(0, 0, 0, 0.04); /* 极轻柔的阴影 */
}

.container {
  box-sizing: border-box;
}

/* 苹果风格通用卡片 */
.ios-card {
  background: var(--card-bg);
  border-radius: var(--radius-lg);
  padding: 30rpx;
  margin-bottom: 30rpx;
  box-shadow: var(--shadow);
}

/* 苹果风格列表组 (如设置页) */
.ios-group {
  background: var(--card-bg);
  border-radius: var(--radius-lg);
  overflow: hidden;
  margin-bottom: 30rpx;
}

/* 点击态反馈 */
.hover-class {
  opacity: 0.7;
  transform: scale(0.98);
  transition: all 0.1s;
}